! Title: MrBukLau's Smart App Banner Removal Filters
! Description: This filter list is for removing Smart App Banners on iOS.
! Homepage: https://github.com/MrBukLau/filter-lists
! License: https://github.com/MrBukLau/filter-lists/blob/master/LICENSE

! Report missing websites at https://github.com/MrBukLau/filter-lists/issues
107ist.org##html > head > meta[name="apple-itunes-app"]:remove()
1112.com##html > head > meta[name="apple-itunes-app"]:remove()
1password.com##html > head > meta[name="apple-itunes-app"]:remove()
4029tv.com##html > head > meta[name="apple-itunes-app"]:remove()
406mtsports.com##html > head > meta[name="apple-itunes-app"]:remove()
49ers.com##html > head > meta[name="apple-itunes-app"]:remove()
53.com##html > head > meta[name="apple-itunes-app"]:remove()
6abc.com##html > head > meta[name="apple-itunes-app"]:remove()
85cbakerycafe.com##html > head > meta[name="apple-itunes-app"]:remove()
9news.com##html > head > meta[name="apple-itunes-app"]:remove()
aa.com##html > head > meta[name="apple-itunes-app"]:remove()
abc.com##html > head > meta[name="apple-itunes-app"]:remove()
abc11.com##html > head > meta[name="apple-itunes-app"]:remove()
abc13.com##html > head > meta[name="apple-itunes-app"]:remove()
abc7news.com##html > head > meta[name="apple-itunes-app"]:remove()
acehardware.com##html > head > meta[name="apple-itunes-app"]:remove()
acolorstory.com##html > head > meta[name="apple-itunes-app"]:remove()
adguard-vpn.com##html > head > meta[name="apple-itunes-app"]:remove()
adguard.com##html > head > meta[name="apple-itunes-app"]:remove()
adweek.com##html > head > meta[name="apple-itunes-app"]:remove()
aeromexico.com##html > head > meta[name="apple-itunes-app"]:remove()
airchina.com.cn##html > head > meta[name="apple-itunes-app"]:remove()
airtransat.com##html > head > meta[name="apple-itunes-app"]:remove()
alaskaair.com##html > head > meta[name="apple-itunes-app"]:remove()
ally.com##html > head > meta[name="apple-itunes-app"]:remove()
amctheatres.com##html > head > meta[name="apple-itunes-app"]:remove()
antiland.com##html > head > meta[name="apple-itunes-app"]:remove()
appadvice.com##html > head > meta[name="apple-itunes-app"]:remove()
appintheair.mobi##html > head > meta[name="apple-itunes-app"]:remove()
apple.com##html > head > meta[name="apple-itunes-app"]:remove()
applebees.com##html > head > meta[name="apple-itunes-app"]:remove()
argusobserver.com##html > head > meta[name="apple-itunes-app"]:remove()
arsenal.com##html > head > meta[name="apple-itunes-app"]:remove()
atomtickets.com##html > head > meta[name="apple-itunes-app"]:remove()
att.com##html > head > meta[name="apple-itunes-app"]:remove()
autonews.com##html > head > meta[name="apple-itunes-app"]:remove()
autozone.com##html > head > meta[name="apple-itunes-app"]:remove()
avfc.co.uk##html > head > meta[name="apple-itunes-app"]:remove()
axs.com##html > head > meta[name="apple-itunes-app"]:remove()
azcentral.com##html > head > meta[name="apple-itunes-app"]:remove()
bakersplus.com##html > head > meta[name="apple-itunes-app"]:remove()
baltimoreravens.com##html > head > meta[name="apple-itunes-app"]:remove()
bankofamerica.com##html > head > meta[name="apple-itunes-app"]:remove()
barstoolsports.com##html > head > meta[name="apple-itunes-app"]:remove()
bedbathandbeyond.com##html > head > meta[name="apple-itunes-app"]:remove()
befunky.com##html > head > meta[name="apple-itunes-app"]:remove()
behance.net##html > head > meta[name="apple-itunes-app"]:remove()
beinsports.com##html > head > meta[name="apple-itunes-app"]:remove()
bengals.com##html > head > meta[name="apple-itunes-app"]:remove()
benzinga.com##html > head > meta[name="apple-itunes-app"]:remove()
bestbuy.com##html > head > meta[name="apple-itunes-app"]:remove()
bet365.com##html > head > meta[name="apple-itunes-app"]:remove()
bhphotovideo.com##html > head > meta[name="apple-itunes-app"]:remove()
bjs.com##html > head > meta[name="apple-itunes-app"]:remove()
bk.com##html > head > meta[name="apple-itunes-app"]:remove()
bloomberg.com##html > head > meta[name="apple-itunes-app"]:remove()
bobevans.com##html > head > meta[name="apple-itunes-app"]:remove()
bokfinancial.com##html > head > meta[name="apple-itunes-app"]:remove()
booking.com##html > head > meta[name="apple-itunes-app"]:remove()
breitbart.com##html > head > meta[name="apple-itunes-app"]:remove()
brentfordfc.com##html > head > meta[name="apple-itunes-app"]:remove()
brightonandhovealbion.com##html > head > meta[name="apple-itunes-app"]:remove()
brueggers.com##html > head > meta[name="apple-itunes-app"]:remove()
buccaneers.com##html > head > meta[name="apple-itunes-app"]:remove()
bumble.com##html > head > meta[name="apple-itunes-app"]:remove()
burgerfi.com##html > head > meta[name="apple-itunes-app"]:remove()
businessinsider.com##html > head > meta[name="apple-itunes-app"]:remove()
busuu.com##html > head > meta[name="apple-itunes-app"]:remove()
buzzfeednews.com##html > head > meta[name="apple-itunes-app"]:remove()
caffenero.com##html > head > meta[name="apple-itunes-app"]:remove()
canadiantire.ca##html > head > meta[name="apple-itunes-app"]:remove()
car-part.com##html > head > meta[name="apple-itunes-app"]:remove()
cars.com##html > head > meta[name="apple-itunes-app"]:remove()
cbc.ca##html > head > meta[name="apple-itunes-app"]:remove()
chargers.com##html > head > meta[name="apple-itunes-app"]:remove()
chase.com##html > head > meta[name="apple-itunes-app"]:remove()
cheddars.com##html > head > meta[name="apple-itunes-app"]:remove()
chicagobears.com##html > head > meta[name="apple-itunes-app"]:remove()
chicagotribune.com##html > head > meta[name="apple-itunes-app"]:remove()
chiefs.com##html > head > meta[name="apple-itunes-app"]:remove()
chiquito.co.uk##html > head > meta[name="apple-itunes-app"]:remove()
cibc.com##html > head > meta[name="apple-itunes-app"]:remove()
cincinnati.com##html > head > meta[name="apple-itunes-app"]:remove()
cineworld.co.uk##html > head > meta[name="apple-itunes-app"]:remove()
citymarket.com##html > head > meta[name="apple-itunes-app"]:remove()
classdojo.com##html > head > meta[name="apple-itunes-app"]:remove()
clevelandbrowns.com##html > head > meta[name="apple-itunes-app"]:remove()
clutchpoints.com##html > head > meta[name="apple-itunes-app"]:remove()
cnbc.com##html > head > meta[name="apple-itunes-app"]:remove()
cnn.com##html > head > meta[name="apple-itunes-app"]:remove()
colts.com##html > head > meta[name="apple-itunes-app"]:remove()
columbian.com##html > head > meta[name="apple-itunes-app"]:remove()
copaair.com##html > head > meta[name="apple-itunes-app"]:remove()
costco.com##html > head > meta[name="apple-itunes-app"]:remove()
cottonon.com##html > head > meta[name="apple-itunes-app"]:remove()
crackle.com##html > head > meta[name="apple-itunes-app"]:remove()
crunchyroll.com##html > head > meta[name="apple-itunes-app"]:remove()
cymera.com##html > head > meta[name="apple-itunes-app"]:remove()
dansdeals.com##html > head > meta[name="apple-itunes-app"]:remove()
deepl.com##html > head > meta[name="apple-itunes-app"]:remove()
deviantart.com##html > head > meta[name="apple-itunes-app"]:remove()
dickeys.com##html > head > meta[name="apple-itunes-app"]:remove()
dillons.com##html > head > meta[name="apple-itunes-app"]:remove()
dominos.com##html > head > meta[name="apple-itunes-app"]:remove()
donatos.com##html > head > meta[name="apple-itunes-app"]:remove()
downdetector.com##html > head > meta[name="apple-itunes-app"]:remove()
dreamstime.com##html > head > meta[name="apple-itunes-app"]:remove()
dropbox.com##html > head > meta[name="apple-itunes-app"]:remove()
dunkindonuts.com##html > head > meta[name="apple-itunes-app"]:remove()
duolingo.com##html > head > meta[name="apple-itunes-app"]:remove()
eastsidemarios.com##html > head > meta[name="apple-itunes-app"]:remove()
emirates.com##html > head > meta[name="apple-itunes-app"]:remove()
espn.com##html > head > meta[name="apple-itunes-app"]:remove()
eurosport.com##html > head > meta[name="apple-itunes-app"]:remove()
evernote.com##html > head > meta[name="apple-itunes-app"]:remove()
express.co.uk##html > head > meta[name="apple-itunes-app"]:remove()
facebook.com##html > head > meta[name="apple-itunes-app"]:remove()
fandango.com##html > head > meta[name="apple-itunes-app"]:remove()
fandom.com##html > head > meta[name="apple-itunes-app"]:remove()
fansided.com##html > head > meta[name="apple-itunes-app"]:remove()
feedly.com##html > head > meta[name="apple-itunes-app"]:remove()
fidelity.com##html > head > meta[name="apple-itunes-app"]:remove()
filemail.com##html > head > meta[name="apple-itunes-app"]:remove()
flickr.com##html > head > meta[name="apple-itunes-app"]:remove()
flightradar24.com##html > head > meta[name="apple-itunes-app"]:remove()
flightstats.com##html > head > meta[name="apple-itunes-app"]:remove()
flyfrontier.com##html > head > meta[name="apple-itunes-app"]:remove()
flytap.com##html > head > meta[name="apple-itunes-app"]:remove()
food4less.com##html > head > meta[name="apple-itunes-app"]:remove()
foodsco.net##html > head > meta[name="apple-itunes-app"]:remove()
fox10tv.com##html > head > meta[name="apple-itunes-app"]:remove()
fox13news.com##html > head > meta[name="apple-itunes-app"]:remove()
fox19.com##html > head > meta[name="apple-itunes-app"]:remove()
fox26houston.com##html > head > meta[name="apple-itunes-app"]:remove()
foxnews.com##html > head > meta[name="apple-itunes-app"]:remove()
foxweather.com##html > head > meta[name="apple-itunes-app"]:remove()
frankieandbennys.com##html > head > meta[name="apple-itunes-app"]:remove()
freddysusa.com##html > head > meta[name="apple-itunes-app"]:remove()
fredmeyer.com##html > head > meta[name="apple-itunes-app"]:remove()
freepeople.com##html > head > meta[name="apple-itunes-app"]:remove()
freshdirect.com##html > head > meta[name="apple-itunes-app"]:remove()
freshii.com##html > head > meta[name="apple-itunes-app"]:remove()
frysfood.com##html > head > meta[name="apple-itunes-app"]:remove()
ft.com##html > head > meta[name="apple-itunes-app"]:remove()
funimation.com##html > head > meta[name="apple-itunes-app"]:remove()
gasbuddy.com##html > head > meta[name="apple-itunes-app"]:remove()
genius.com##html > head > meta[name="apple-itunes-app"]:remove()
getpocket.com##html > head > meta[name="apple-itunes-app"]:remove()
gettyimages.com##html > head > meta[name="apple-itunes-app"]:remove()
gfycat.com##html > head > meta[name="apple-itunes-app"]:remove()
giants.com##html > head > meta[name="apple-itunes-app"]:remove()
github.com##html > head > meta[name="apple-itunes-app"]:remove()
golfchannel.com##html > head > meta[name="apple-itunes-app"]:remove()
google.*##html > head > meta[name="apple-itunes-app"]:remove()
gopuff.com##html > head > meta[name="apple-itunes-app"]:remove()
gorin.jp##html > head > meta[name="apple-itunes-app"]:remove()
grammarly.com##html > head > meta[name="apple-itunes-app"]:remove()
greatukpubs.co.uk##html > head > meta[name="apple-itunes-app"]:remove()
gyazo.com##html > head > meta[name="apple-itunes-app"]:remove()
haaretz.com##html > head > meta[name="apple-itunes-app"]:remove()
happn.com##html > head > meta[name="apple-itunes-app"]:remove()
harristeeter.com##html > head > meta[name="apple-itunes-app"]:remove()
harveys.ca##html > head > meta[name="apple-itunes-app"]:remove()
hawaiianairlines.com##html > head > meta[name="apple-itunes-app"]:remove()
hawaiinewsnow.com##html > head > meta[name="apple-itunes-app"]:remove()
heraldsun.com.au##html > head > meta[name="apple-itunes-app"]:remove()
hl.co.uk##html > head > meta[name="apple-itunes-app"]:remove()
hm.com##html > head > meta[name="apple-itunes-app"]:remove()
houstontexans.com##html > head > meta[name="apple-itunes-app"]:remove()
hsbc.*##html > head > meta[name="apple-itunes-app"]:remove()
huffpost.com##html > head > meta[name="apple-itunes-app"]:remove()
hungryhowies.com##html > head > meta[name="apple-itunes-app"]:remove()
ibispaint.com##html > head > meta[name="apple-itunes-app"]:remove()
igeeksblog.com##html > head > meta[name="apple-itunes-app"]:remove()
ihop.com##html > head > meta[name="apple-itunes-app"]:remove()
ikea.com##html > head > meta[name="apple-itunes-app"]:remove()
indystar.com##html > head > meta[name="apple-itunes-app"]:remove()
inoreader.com##html > head > meta[name="apple-itunes-app"]:remove()
instapaper.com##html > head > meta[name="apple-itunes-app"]:remove()
investing.com##html > head > meta[name="apple-itunes-app"]:remove()
investors.com##html > head > meta[name="apple-itunes-app"]:remove()
irishtimes.com##html > head > meta[name="apple-itunes-app"]:remove()
istockphoto.com##html > head > meta[name="apple-itunes-app"]:remove()
jamba.com##html > head > meta[name="apple-itunes-app"]:remove()
jango.com##html > head > meta[name="apple-itunes-app"]:remove()
jaycfoods.com##html > head > meta[name="apple-itunes-app"]:remove()
jollibeefoods.com##html > head > meta[name="apple-itunes-app"]:remove()
kbtx.com##html > head > meta[name="apple-itunes-app"]:remove()
kcci.com##html > head > meta[name="apple-itunes-app"]:remove()
kcra.com##html > head > meta[name="apple-itunes-app"]:remove()
keepersecurity.com##html > head > meta[name="apple-itunes-app"]:remove()
kelseys.ca##html > head > meta[name="apple-itunes-app"]:remove()
ketv.com##html > head > meta[name="apple-itunes-app"]:remove()
kgw.com##html > head > meta[name="apple-itunes-app"]:remove()
khou.com##html > head > meta[name="apple-itunes-app"]:remove()
king5.com##html > head > meta[name="apple-itunes-app"]:remove()
kingsoopers.com##html > head > meta[name="apple-itunes-app"]:remove()
kmbc.com##html > head > meta[name="apple-itunes-app"]:remove()
koat.com##html > head > meta[name="apple-itunes-app"]:remove()
koco.com##html > head > meta[name="apple-itunes-app"]:remove()
kptv.com##html > head > meta[name="apple-itunes-app"]:remove()
kroger.com##html > head > meta[name="apple-itunes-app"]:remove()
ksbw.com##html > head > meta[name="apple-itunes-app"]:remove()
kstp.com##html > head > meta[name="apple-itunes-app"]:remove()
ky3.com##html > head > meta[name="apple-itunes-app"]:remove()
livehealthily.com##html > head > meta[name="apple-itunes-app"]:remove()
localmemphis.com##html > head > meta[name="apple-itunes-app"]:remove()
loopnet.com##html > head > meta[name="apple-itunes-app"]:remove()
lovoo.com##html > head > meta[name="apple-itunes-app"]:remove()
lufthansa.com##html > head > meta[name="apple-itunes-app"]:remove()
mancity.com##html > head > meta[name="apple-itunes-app"]:remove()
manutd.com##html > head > meta[name="apple-itunes-app"]:remove()
marca.com##html > head > meta[name="apple-itunes-app"]:remove()
marketbeat.com##html > head > meta[name="apple-itunes-app"]:remove()
marketwatch.com##html > head > meta[name="apple-itunes-app"]:remove()
maxburgers.com##html > head > meta[name="apple-itunes-app"]:remove()
maxpreps.com##html > head > meta[name="apple-itunes-app"]:remove()
medscape.com##html > head > meta[name="apple-itunes-app"]:remove()
meetcarrot.com##html > head > meta[name="apple-itunes-app"]:remove()
meetme.com##html > head > meta[name="apple-itunes-app"]:remove()
mega.*##html > head > meta[name="apple-itunes-app"]:remove()
menards.com##html > head > meta[name="apple-itunes-app"]:remove()
miamidolphins.com##html > head > meta[name="apple-itunes-app"]:remove()
miamiherald.com##html > head > meta[name="apple-itunes-app"]:remove()
mirror.co.uk##html > head > meta[name="apple-itunes-app"]:remove()
momondo.com##html > head > meta[name="apple-itunes-app"]:remove()
moneycontrol.com##html > head > meta[name="apple-itunes-app"]:remove()
montanas.ca##html > head > meta[name="apple-itunes-app"]:remove()
mor-tv.com##html > head > meta[name="apple-itunes-app"]:remove()
mtygroup.com##html > head > meta[name="apple-itunes-app"]:remove()
musixmatch.com##html > head > meta[name="apple-itunes-app"]:remove()
mynbc5.com##html > head > meta[name="apple-itunes-app"]:remove()
napavalleyregister.com##html > head > meta[name="apple-itunes-app"]:remove()
nbc12.com##html > head > meta[name="apple-itunes-app"]:remove()
nbcolympics.com##html > head > meta[name="apple-itunes-app"]:remove()
ndtv.com##html > head > meta[name="apple-itunes-app"]:remove()
nesn.com##html > head > meta[name="apple-itunes-app"]:remove()
neworleanssaints.com##html > head > meta[name="apple-itunes-app"]:remove()
news360.com##html > head > meta[name="apple-itunes-app"]:remove()
newyorkjets.com##html > head > meta[name="apple-itunes-app"]:remove()
nick.com##html > head > meta[name="apple-itunes-app"]:remove()
nordstrom.com##html > head > meta[name="apple-itunes-app"]:remove()
ntd.com##html > head > meta[name="apple-itunes-app"]:remove()
nypost.com##html > head > meta[name="apple-itunes-app"]:remove()
olympics.com##html > head > meta[name="apple-itunes-app"]:remove()
omnigroup.com##html > head > meta[name="apple-itunes-app"]:remove()
onlinepianist.com##html > head > meta[name="apple-itunes-app"]:remove()
ontvtonight.com##html > head > meta[name="apple-itunes-app"]:remove()
outback.com##html > head > meta[name="apple-itunes-app"]:remove()
packers.com##html > head > meta[name="apple-itunes-app"]:remove()
packpnt.com##html > head > meta[name="apple-itunes-app"]:remove()
panago.com##html > head > meta[name="apple-itunes-app"]:remove()
panthers.com##html > head > meta[name="apple-itunes-app"]:remove()
papajohns.com##html > head > meta[name="apple-itunes-app"]:remove()
patriots.com##html > head > meta[name="apple-itunes-app"]:remove()
pay-less.com##html > head > meta[name="apple-itunes-app"]:remove()
pcalc.com##html > head > meta[name="apple-itunes-app"]:remove()
pcloud.com##html > head > meta[name="apple-itunes-app"]:remove()
philadelphiaeagles.com##html > head > meta[name="apple-itunes-app"]:remove()
picmonkey.com##html > head > meta[name="apple-itunes-app"]:remove()
planefinder.net##html > head > meta[name="apple-itunes-app"]:remove()
polarr.com##html > head > meta[name="apple-itunes-app"]:remove()
popeyes.com##html > head > meta[name="apple-itunes-app"]:remove()
popsugar.com##html > head > meta[name="apple-itunes-app"]:remove()
portillos.com##html > head > meta[name="apple-itunes-app"]:remove()
post-gazette.com##html > head > meta[name="apple-itunes-app"]:remove()
premierleague.com##html > head > meta[name="apple-itunes-app"]:remove()
pressherald.com##html > head > meta[name="apple-itunes-app"]:remove()
pure.app##html > head > meta[name="apple-itunes-app"]:remove()
qeep.net##html > head > meta[name="apple-itunes-app"]:remove()
qfc.com##html > head > meta[name="apple-itunes-app"]:remove()
racingtv.com##html > head > meta[name="apple-itunes-app"]:remove()
raiders.com##html > head > meta[name="apple-itunes-app"]:remove()
ralphs.com##html > head > meta[name="apple-itunes-app"]:remove()
regions.com##html > head > meta[name="apple-itunes-app"]:remove()
regmovies.com##html > head > meta[name="apple-itunes-app"]:remove()
replicaapp.com##html > head > meta[name="apple-itunes-app"]:remove()
resy.com##html > head > meta[name="apple-itunes-app"]:remove()
reuters.com##html > head > meta[name="apple-itunes-app"]:remove()
roblox.com##html > head > meta[name="apple-itunes-app"]:remove()
roboform.com##html > head > meta[name="apple-itunes-app"]:remove()
rome2rio.com##html > head > meta[name="apple-itunes-app"]:remove()
rootsports.com##html > head > meta[name="apple-itunes-app"]:remove()
royrogersrestaurants.com##html > head > meta[name="apple-itunes-app"]:remove()
rsi.ch##html > head > meta[name="apple-itunes-app"]:remove()
runtastic.com##html > head > meta[name="apple-itunes-app"]:remove()
saksfifthavenue.com##html > head > meta[name="apple-itunes-app"]:remove()
salon.com##html > head > meta[name="apple-itunes-app"]:remove()
salsaritas.com##html > head > meta[name="apple-itunes-app"]:remove()
schlotzskys.com##html > head > meta[name="apple-itunes-app"]:remove()
scmp.com##html > head > meta[name="apple-itunes-app"]:remove()
seahawks.com##html > head > meta[name="apple-itunes-app"]:remove()
sent0.github.io##html > head > meta[name="apple-itunes-app"]:remove()
shazam.com##html > head > meta[name="apple-itunes-app"]:remove()
shutterstock.com##html > head > meta[name="apple-itunes-app"]:remove()
signal.org##html > head > meta[name="apple-itunes-app"]:remove()
singaporeair.com##html > head > meta[name="apple-itunes-app"]:remove()
skimble.com##html > head > meta[name="apple-itunes-app"]:remove()
skysports.com##html > head > meta[name="apple-itunes-app"]:remove()
slugandlettuce.co.uk##html > head > meta[name="apple-itunes-app"]:remove()
smithsfoodanddrug.com##html > head > meta[name="apple-itunes-app"]:remove()
snapfish.com##html > head > meta[name="apple-itunes-app"]:remove()
sofascore.com##html > head > meta[name="apple-itunes-app"]:remove()
sonicdrivein.com##html > head > meta[name="apple-itunes-app"]:remove()
southamptonfc.com##html > head > meta[name="apple-itunes-app"]:remove()
southwest.com##html > head > meta[name="apple-itunes-app"]:remove()
spectrum.com##html > head > meta[name="apple-itunes-app"]:remove()
sportsmockery.com##html > head > meta[name="apple-itunes-app"]:remove()
starz.com##html > head > meta[name="apple-itunes-app"]:remove()
statnews.com##html > head > meta[name="apple-itunes-app"]:remove()
steaknshake.com##html > head > meta[name="apple-itunes-app"]:remove()
steelers.com##html > head > meta[name="apple-itunes-app"]:remove()
stickypassword.com##html > head > meta[name="apple-itunes-app"]:remove()
stltoday.com##html > head > meta[name="apple-itunes-app"]:remove()
swiss.com##html > head > meta[name="apple-itunes-app"]:remove()
swisschalet.com##html > head > meta[name="apple-itunes-app"]:remove()
t-mobilemoney.com##html > head > meta[name="apple-itunes-app"]:remove()
t.me##html > head > meta[name="apple-itunes-app"]:remove()
tacobell.com##html > head > meta[name="apple-itunes-app"]:remove()
target.com##html > head > meta[name="apple-itunes-app"]:remove()
teamviewer.com##html > head > meta[name="apple-itunes-app"]:remove()
telegram.org##html > head > meta[name="apple-itunes-app"]:remove()
telemundo.com##html > head > meta[name="apple-itunes-app"]:remove()
tennesseetitans.com##html > head > meta[name="apple-itunes-app"]:remove()
tenor.com##html > head > meta[name="apple-itunes-app"]:remove()
thaiairways.com##html > head > meta[name="apple-itunes-app"]:remove()
theathletic.com##html > head > meta[name="apple-itunes-app"]:remove()
thehill.com##html > head > meta[name="apple-itunes-app"]:remove()
therams.com##html > head > meta[name="apple-itunes-app"]:remove()
tickpick.com##html > head > meta[name="apple-itunes-app"]:remove()
ticktick.com##html > head > meta[name="apple-itunes-app"]:remove()
timhortons.com##html > head > meta[name="apple-itunes-app"]:remove()
tmz.com##html > head > meta[name="apple-itunes-app"]:remove()
transitapp.com##html > head > meta[name="apple-itunes-app"]:remove()
trello.com##html > head > meta[name="apple-itunes-app"]:remove()
tripadvisor.com##html > head > meta[name="apple-itunes-app"]:remove()
tripit.com##html > head > meta[name="apple-itunes-app"]:remove()
tropicalsmoothiecafe.com##html > head > meta[name="apple-itunes-app"]:remove()
tubitv.com##html > head > meta[name="apple-itunes-app"]:remove()
turkishairlines.com##html > head > meta[name="apple-itunes-app"]:remove()
tvguide.com##html > head > meta[name="apple-itunes-app"]:remove()
twoo.com##html > head > meta[name="apple-itunes-app"]:remove()
udemy.com##html > head > meta[name="apple-itunes-app"]:remove()
un.org##html > head > meta[name="apple-itunes-app"]:remove()
united.com##html > head > meta[name="apple-itunes-app"]:remove()
unsplash.com##html > head > meta[name="apple-itunes-app"]:remove()
urbanoutfitters.com##html > head > meta[name="apple-itunes-app"]:remove()
usatoday.com##html > head > meta[name="apple-itunes-app"]:remove()
variety.com##html > head > meta[name="apple-itunes-app"]:remove()
vice.com##html > head > meta[name="apple-itunes-app"]:remove()
videolan.org##html > head > meta[name="apple-itunes-app"]:remove()
vikings.com##html > head > meta[name="apple-itunes-app"]:remove()
vitacost.com##html > head > meta[name="apple-itunes-app"]:remove()
vk.com##html > head > meta[name="apple-itunes-app"]:remove()
vmagazine.com##html > head > meta[name="apple-itunes-app"]:remove()
voanews.com##html > head > meta[name="apple-itunes-app"]:remove()
vrv.co##html > head > meta[name="apple-itunes-app"]:remove()
wafb.com##html > head > meta[name="apple-itunes-app"]:remove()
waff.com##html > head > meta[name="apple-itunes-app"]:remove()
walkaboutbars.co.uk##html > head > meta[name="apple-itunes-app"]:remove()
walmart.com##html > head > meta[name="apple-itunes-app"]:remove()
wapt.com##html > head > meta[name="apple-itunes-app"]:remove()
wbaltv.com##html > head > meta[name="apple-itunes-app"]:remove()
wbtv.com##html > head > meta[name="apple-itunes-app"]:remove()
wcax.com##html > head > meta[name="apple-itunes-app"]:remove()
wcvb.com##html > head > meta[name="apple-itunes-app"]:remove()
wdsu.com##html > head > meta[name="apple-itunes-app"]:remove()
weatherbug.com##html > head > meta[name="apple-itunes-app"]:remove()
wellsfargo.com##html > head > meta[name="apple-itunes-app"]:remove()
wesh.com##html > head > meta[name="apple-itunes-app"]:remove()
wgal.com##html > head > meta[name="apple-itunes-app"]:remove()
whichwich.com##html > head > meta[name="apple-itunes-app"]:remove()
whisper.sh##html > head > meta[name="apple-itunes-app"]:remove()
wish.com##html > head > meta[name="apple-itunes-app"]:remove()
wisn.com##html > head > meta[name="apple-itunes-app"]:remove()
wistv.com##html > head > meta[name="apple-itunes-app"]:remove()
witn.com##html > head > meta[name="apple-itunes-app"]:remove()
wjcl.com##html > head > meta[name="apple-itunes-app"]:remove()
wlky.com##html > head > meta[name="apple-itunes-app"]:remove()
wlwt.com##html > head > meta[name="apple-itunes-app"]:remove()
wmtw.com##html > head > meta[name="apple-itunes-app"]:remove()
wmur.com##html > head > meta[name="apple-itunes-app"]:remove()
wolves.co.uk##html > head > meta[name="apple-itunes-app"]:remove()
wpbf.com##html > head > meta[name="apple-itunes-app"]:remove()
wsmv.com##html > head > meta[name="apple-itunes-app"]:remove()
wtae.com##html > head > meta[name="apple-itunes-app"]:remove()
wvtm13.com##html > head > meta[name="apple-itunes-app"]:remove()
wxii12.com##html > head > meta[name="apple-itunes-app"]:remove()
wyff4.com##html > head > meta[name="apple-itunes-app"]:remove()
yahoo.com##html > head > meta[name="apple-itunes-app"]:remove()
yelp.com##html > head > meta[name="apple-itunes-app"]:remove()
yogurt-land.com##html > head > meta[name="apple-itunes-app"]:remove()
zoom.us##html > head > meta[name="apple-itunes-app"]:remove()
